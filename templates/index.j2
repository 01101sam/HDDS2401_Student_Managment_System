<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="ISO-8859-1">
    <title>SMY | List</title>
    <link rel="stylesheet"
          href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
          integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
          crossorigin="anonymous">
</head>
<body>
<div class="container my-2" align="center">
    <h3>Student List</h3>
    <a href="/new" class="btn btn-primary btn-sm mb-3">Add</a>
    {% if student_list %}
        <table style="width:80%" border="1" class="table table-striped table-responsive-md">
            <thead>
            <tr>
                <th>Name</th>
                <th>Gender</th>
                <th>Birth</th>
                <th>Study Year</th>
                <th>Class ID</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                {% for student in student_list %}
                    <td>{{ student.name }}</td>
                    <td>{{ student.gender }}</td>
                    <td>{{ student.birth }}</td>
                    <td>{{ student.study_year }}</td>
                    <td>{{ student.class_id }}</td>
                    <td>
                        <a href="/update/{{ student.id or '' }}" class="btn btn-primary">Update</a>
                        <a href="/delete/{{ student.id or '' }}" class="btn btn-danger">Delete</a>
                    </td>
                {% endfor %}
            </tr>
            </tbody>
        </table>
    {% else %}
        <p>No student record found.</p>
    {% endif %}
</div>
</body>
</html>
